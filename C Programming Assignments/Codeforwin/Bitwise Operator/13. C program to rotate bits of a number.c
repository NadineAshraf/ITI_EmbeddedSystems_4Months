#include <stdio.h>
int rotateRight(int num, int rotations)
{
    int Dropped_LSB;
    num>>=1;
    num = num & (~)
}
int main()
{
   int num,i,rotations;
   printf("Input number = ");
   scanf("%d",&num);
   printf("Number of rotations = ");
   scanf("%d",&rotations);
 
  
   printf("%d left rotated %d times = %d\n",num,rotations,);
   printf("%d right rotated %d times = %d",num,rotations,);
   
   
    return 0;
}
